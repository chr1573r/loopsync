# global.cfg - Global options for loopsync.sh
# This file is not neccessary for operation controlled by cron etc.
# loopsync will use defaults where necessary

# The variables below can be uncommented (removing first "#" in each line) if you wish to alter loopsync's default behavior (similar to that of v1)
#CHECKINTERVAL=3600		# Disabled by default. Default value: 3600. Specifies number of seconds loopsync will wait before checking loopsleep.txt when in sleepmode.

#AUTOWAKEUP=SET			# Disabled by default. Default value: SET. Is enabled if set to anything non-empty. If enabled, loopsync will wake up automatically from sleep after number of seconds specified by CHECKINTERVAL

#DISABLEAUTOSLEEP=SET   # Disabled by default. Default value: SET. Is enabled if set to anything non-empty. If enabled, loopsync will not automatically generate the loopsleep.txt file which triggers sleepmode